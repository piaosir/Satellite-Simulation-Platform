# 卫星仿真平台 — 开发约定

## 界面：不写解释性文字

**界面上不出现「类似注释」的说明文字。** 这类文字不参与任何计算，纯占版面。

不要写：

- 面板底部 / 分区末尾的口径解释段落（`.tip` `.note` `.hint` `.lead` `.pw-note` `.ci-note` 一类）
- 跟在输入框后面的小注（`<em>解析式在方向图零点会掉到 −∞，下界须显式给定</em>`）
- 空态里的操作教学（「还没有转发器。用右侧『批量生成』按『起始频率 + 频率间隔 + 数量』一次铺一排，或点上方『转发器』逐个加。」）
- 图表下方的读图指南（「纵轴取对数刻度：关注区间 0.001%–1%…竖线为门限，交点即…」）

可以留：

- 悬停 `title`（不占版面）——字段口径说明放这里
- 含运行时数据的读数行（「共 12 个存活波束」「雨衰分布：xxx」「耗时 340 ms」）
- 错误与告警，但只留状态本身，砍掉教学式从句（「未计入卫星发射方向图。」够了，不必解释这为什么是上界包络）
- 空态一句陈述句：「还没有转发器。」「暂无 Polygon。」「尚无预算结果。」

判据：这行字删掉后界面还能不能用？能 → 删。

工程说明写代码注释，不写进 UI。

**改动注意**：删带 `v-if` 的说明元素时，紧跟其后的 `v-else` / `v-else-if` 会断链，Vue 编译报 `Cannot read properties of undefined (reading 'type')`。删完跑 `npm run build`。

## 结果输出：纯数字

计算结果区不出现文字判定（「达标」「受限」「合格」「资源判定」等）。数值着色与报错诊断保留。
