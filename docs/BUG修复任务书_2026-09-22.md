# BUG 修复任务书（v1.4.11 → 当前工作区审查结论）

日期：2026-09-22　仓库：`C:\Users\85256\WeChatProjects\卫星仿真平台`　分支：main

本任务书由一轮代码审查产出：对比 v1.4.11 标签到当前工作区的 42 个提交 + 未提交改动，单测 129 文件全绿、`npm run build` 通过，但读代码找出下列缺陷。**前 10 条已逐行核实，11–14 条读代码判定「很可能」，15–20 条为次要问题。** 行号以 2026-09-22 的工作树为准，改动前先 grep 确认。

---

## 0. 开工前必读

### 0.1 工作区现状与并发规则

- 工作区里有一批**未提交**改动（覆盖分析 2D 等值线提速 / Whittaker 密度 / 画法回退），属于另一条工作线，**不要动、不要回退、不要提交它们**。涉及文件：`package.json`、`packages/core/test/covRealGrd.test.mjs`、`src/components/GrdSetSections.vue`、`src/perf/GroundPerfWin.vue`、`src/shared/cmdIndex.data.js`、`src/shared/i18n/uiDict.data.js`、`src/stores/viewPrefs.js`、`src/viz/env/useEnvField.js`、`src/viz/flatmap/flatCoverage.js`、`src/viz/flatmap/glField.js`、`src/viz/geo/islandChains.js`、`src/viz/globe3d/scene.js`、`src/viz/grd/coverage.js`、`src/viz/grd/perfWinHost.js`、`src/viz/grd/useBeamSynth.js`、`src/viz/grd/useGrdCoverage.js`、`src/viz/grd/usePerfTable.js`、`src/viz/grd/useShellCoverage.js`、`src/viz/vis/useVisibility.js`，以及未跟踪的 `src/viz/grd/whittaker.js`、`packages/core/test/whittaker.test.mjs`、`.covharness/`、`.layersharness/`、`.owizharness/`、两份 `docs/*任务书_2026-09-21.md`。
- 本任务书的修复**不需要碰上面任何一个文件**（第 21 条改 `.gitignore` 除外）。提交一律 `git add <明确路径>`，**禁止 `git add -A` / `git add .`**；每次提交后 `git status` 里上面那批文件必须仍是 `M` / `??`。
- 建议按条目或按模块拆提交，提交信息用中文、写清「现象 → 修法」，风格参照 `git log --oneline -30`。

### 0.2 项目铁律（违反即返工）

- `CLAUDE.md`：界面上不写解释性文字（`.tip/.note/.hint` 一类一律不加），空态只留一句陈述；计算结果区不出现文字判定。字段口径说明只能放 `title`。
- IPC 不能传 Vue 响应式代理（结构化克隆会抛且静默）；出 IPC 前造纯数据。
- `src/pages/ConstellationMap3D.vue` 与 `src/App.vue` 是 **CRLF**，用 Edit 工具改，不要用 sed / heredoc 整段重写。
- 渲染端不能 import `packages/core` 里的 CJS（Vite 不转 CJS）。`src/viz/constellation/ephemTable.js` 是 `packages/core/utils/ephemInterp.js` 的镜像，两边取位逻辑必须逐字一致；改一处必须同步另一处并跑 `ephemParity.test.mjs`。
- 删带 `v-if` 的元素后，后面的 `v-else / v-else-if` 会断链，改完模板必须 `npm run build`。
- 新增界面文案要补 `src/shared/i18n/uiDict.data.js` 的英文词条（`uiDict.test.mjs` 会拦缺口）。
- 不要顺手重构、不要改无关代码、不要重建「已回退模块清单」里的东西。

### 0.3 验证基线

```bash
npm test
```
基线 129 个测试文件全绿。

```bash
npm run build
```
必须通过。每条修复都要补单测或改现有单测，能在 node 里复现的先写失败用例再修。

---

## 1. P0：必修（前 10 条已核实）

### 1. SEM 年历每颗星按 13 个数切，真实文件是 14 个

- 文件：`packages/core/utils/gpsAlmanac.js:117`（`const PER = 13`）及其后的字段映射（:122–135）。
- 根因：ICD-GPS-240 的 SEM 记录每颗星 14 个数：PRN、SVN、URA、e、δi（半圆，相对 0.30）、Ω̇、√A、Ω₀、ω、M₀、af0、af1、health、**config**。代码按数值流每 13 个切一颗，从第 2 颗起全部错位（第 2 颗的 PRN 读到上一颗的 config）。注释里「config 某些产品缺省，故按 13 取、多的跳过」在流式切分下不成立。
- 修法：`PER = 14`；兼容旧变体：若 `nums.length === count * 13`（头里声明的记录数）则退回 13，否则 14；两者都对不上时按 14 并告警「记录数与字段数对不上」。
- 验收：`packages/core/test/gpsAlmanac.test.mjs` 里的 `semText` 夹具改成真实 CelesTrak SEM 体例（≥3 颗、每颗 14 个数，含 config），断言第 2、3 颗的 PRN / 倾角 / √A 正确；保留一条 13 字段变体用例。真实文件可从 https://celestrak.org/GPS/almanac/SEM/ 取（`curl` 带浏览器 UA）。

### 2. 年历反查 NORAD 的正则丢了反斜杠

- 文件：`electron/services/customSats.js:212`：`/(PRNs*(d+))/i`。
- 根因：应为 `/PRN\s*(\d+)/i`。现在匹配的是字面量 `PRN` + 若干 `s` + 若干 `d`，对「GPS BIIR-2 (PRN 13)」永远不命中；即便命中 `m[1]` 也不是数字。结果：导入 YUMA/SEM 时「NORAD 反查 0 / N 命中」，全部给合成号 9900xx。
- 修法：改正则并取 `m[1]`。`scripts/verify-accept.cjs` 第 ③ 项自带一条正确正则可参照。
- 验收：新增单测（可放 `customEphemStore.test.mjs`）：名字 `GPS BIIR-2  (PRN 13)` → 13；`NAVSTAR 43 (USA 132)` → 无匹配；用随包 `resources/omm/csv_gps.csv` 跑一遍反查命中数 ≥ 30。

### 3. 星间链路求解器读星历表的 `jdsatepoch`

- 文件：`packages/core/utils/ngsoGeometry.js:852` 与 `:995`（两处 `epochOf`）。
- 根因：星历点序列表（`__ephem`）没有 `jdsatepoch`，`new Date(NaN)`。不带 `t0ISO` 调 `solveIslWorstCase` / `sampleIslRangeSeries` 直接抛 `RangeError: Invalid time value`；再生式 / 端到端窗口用墙钟做 `t0ISO` 时，导入星历时段通常不含「现在」，逐拍全 null，最后误报「搜索时窗内两星从不互视」。站-星那两条路（:460、:737）已经用 `anchorDate()` + `ephemSpanClip()`，星间路没跟上。
- 修法：`epochOf` 改用同文件的 `anchorDate(nd.node.satrec)`；两条星间路都加 `ephemSpanClip`：时窗取两颗星时段的交集，无交集时返回与站-星路同款的 `reason`（「星历时段 … 与分析时段 … 不重叠」），不能落到「从不互视」。
- 验收：`packages/core/test/ephemParity.test.mjs` 或 `islGeometry.test.mjs` 加用例：两颗星都用 `buildTable` 造的表，不带 `t0ISO` 调用不抛；`t0ISO` 落在时段外 → `reason` 含「不重叠」；时段内 → 正常出结果。

### 4. 3D 页信息卡与聚焦几何直接读 `rec.no`，星历点序列星没有 `rec`

- 文件：`src/pages/ConstellationMap3D.vue:1800`（`cardFor`：`const rec = e.rec; (2π)/rec.no …`）、`:1909`（`startFocusGeometry` 的 TTL：`draw[i].rec.no`）；Worker 侧 `src/viz/constellation/focusGeomTick.js:76`、`src/viz/constellation/focusGeomCache.js:59` 同样从 `rec.no` 取周期。
- 现象：导入 .e 组并显示 → 点它的星点 → `selectSat → refreshSelection → cardFor` 抛 TypeError，之后时钟每拍再抛；聚焦它时轨道线 / 星下点轨迹画不出。
- 修法：
  1. 在 `src/viz/constellation/satPos.js` 加 `periodMinOf(x)`：satrec → `2π/no`；星历表 → `estimatePeriodMin(tab)`（`ephemTable.js` 里已写好、无人调用），估不出返回 null。
  2. `cardFor`：星历星走 `posAt` 取高度 / 经纬 / 速度，周期用 `periodMinOf`，区制用周期推 `aKm` 后 `classifyOrbit`；根数字段（倾角 / 偏心率 / RAAN / 近地点幅角 / 平近点角 / 近远地点）没有就留空，不编数。`slot` 的 `geoSlotOfSatrec(e.rec)` 要判空。
  3. `startFocusGeometry` 与两个 Worker 文件：周期统一走 `periodMinOf`；Worker 的传播改走 `posAtMs`（表对象是纯数据 + typed array，能结构化克隆进 Worker），估不出周期的星历星，轨道线 / 轨迹直接按表点连线（表本身就是轨迹）。
  4. 顺手 grep `\.rec\.no\|rec\.no\b` 把同类直接解引用一并收口。
- 验收：导入 .e 组，点星点 → 卡片出、控制台零报错；双击聚焦 → 轨道线 / 轨迹 / 覆盖圈能画；时钟播放不报错。

### 5. 渲染端装表只认 `sg` 分段字段，主进程发的是 `spans`

- 文件：`src/viz/constellation/ephemTable.js:104–124`（`tableFrom` 只读 `raw.sg/sgA/sgB`）；`electron/services/customSats.js:457–463`（`ephemTable()` 发 `spans: s.spans || null`）；分段表的构造逻辑在 `packages/core/utils/ephemInterp.js:93–108`（`buildTable` 内部）。
- 现象：多段 OEM（段间有空档）在主进程各引擎里正确返回 null，3D 图却跨段 Lagrange 插出假位置。
- 修法：把 `ephemInterp.js:93–108` 那段抽成导出函数 `segmentTables(t, spans) → { sg, sgA, sgB }`（`buildTable` 改为调用它，行为不变）；`customSats.ephemTable()` 对每颗星调用它，把三张 `Int32Array` 随 `t/p/v` 一起发；渲染端 `tableFrom` 不用改。**不要动 `ephemTable.js` 的 `evalTable` / `findIndex`**（镜像铁律）。
- 验收：`customEphemStore.test.mjs` 加用例：两段、中间空 1 h 的表经 `ephemTable()` 路径 → payload 带 `sg`；再喂 `tableFrom` + `evalTable`，缝里取位为 null，段内正常；`ephemParity.test.mjs` 仍绿。

### 6. 「查找卫星」几何五项读的字段搜索池记录里没有

- 文件：`src/shared/satFilter.js:64–90`（`metricsOf` 读 `rec.incl / meanMotion / ecc / apogeeKm / perigeeKm`）；`src/pages/ConstellationMap3D.vue:2461`（`setSearchPool` 的记录只有 `{ rec, name, noradId, group }`，根数在 `rec.rec.inclo / no / ecco / alta / altp`）；调用点 `:3326–3331`、`:3464–3468`。
- 现象：没下载编目时按倾角筛 → 命中 0 颗；有编目时不在编目里的星（自定义 / 导入 / 向导生成）全被筛掉。
- 修法：新建 `src/shared/satrecMetrics.js`：`metricsFromSatrec(satrec)` → `{ incl: inclo/DEG, periodMin: 2π/no, meanMotion: 1440/periodMin, ecc: ecco, apogeeKm: alta·RE, perigeeKm: altp·RE }`（RE = 6378.137）。`metricsOf` 先认 `rec.rec`（satrec）再认现有两种形状；星历星（`rec.eph`）周期走 `periodMinOf`，近远地点可由表内地心距极值减 RE 得到，得不到就 null。`cardFor`（:1800–1810）改用同一个 helper，只留一份算式。
- 验收：`packages/core/test/satFilter.test.mjs` 加用例：池形记录 `{ rec: omm2satrec(...), noradId }`，无编目时倾角 / 周期 / 近远地点 / 区制筛选命中；有编目但该星不在编目里时仍命中。

### 7. 轨道向导按仿真时钟时刻解 RAAN，根数却钉在场景历元

- 文件：`src/pages/ConstellationMap3D.vue:1611–1614`（`constSolved`：`t0 = ccTimeAt().getTime()`，`ccTimeAt` 就是时钟）；根数落库在 `src/viz/constellation/useCustomConstellations.js:101`（`elementsToSatrec(s.elements, scenarioEpoch.value)`，场景历元默认当天 08:00）。
- 根因：经度类轨道（地球同步 / Molniya 远地点经度 / 回归 / 临界 / 升交点经度）用 `GMST(时钟)` 把经度换成 RAAN，但平近点角是从场景历元起算的，到时钟时刻已多走了 n·(时钟 − 历元)。地球同步轨道 λ 偏 15°/h × (时钟 − 历元)；读数区用同一个错 GMST 反算，自洽所以看不出。
- 修法：`t0 = new Date(customConst.scenarioEpoch.value).getTime()`（无效时才退回 `calcAt()`），`gmst = sat.gstime(new Date(t0))`；读数区标明是场景历元时刻的值（放 `title`，不加说明文字）。附带收益：`constSolved` 不再每拍重算。
- 顺手修第 20 条：`src/viz/constellation/orbitDesign.js:406` 的 `subLonDeg = raanToLon(Ω+ω+M₀)` 忽略倾角，应为 λ = Ω + atan2(cos i · sin u, cos u) − GMST，u = ω + ν（ν 由 M 解 Kepler 方程；e≈0 时 ν≈M）。
- 验收：`packages/core/test/orbitDesign.test.mjs` 加用例：场景历元 E，地球同步 λ = 110.5°E → `solveDesign(..., E, {gmst: gstime(E)})` → 按 E 建 satrec → 传播到 E + 12 h，星下点经度 110.5 ± 0.1°。手工：历元 08:00、时钟拖到 20:00 生成，球上星就在 110.5°E。

### 8. 双击导入星历组里的卫星聚焦 → 「不在当前星历中」

- 文件：`src/pages/ConstellationMap3D.vue:2975–2979`（`expLocate` 只在 `searchSource()` 里找）、`:2865–2896`（`expResolve` 只有 `c:` / `g:` 分支，`i:` 落到全量池）、`:7217–7223`（`impSatList` 用 `s.noradId` 当行 id，星历星常常没有 NORAD）。
- 修法：`expResolve` / `expLocate` 加 `tag[0] === 'i'` 分支：星历组从 `ephemEntriesOf(gid)`（或已缓存的 `ephTables`）里取 entry，先按 `noradId`、再按 `key` / `name` 匹配；gp 类导入组池里找不到时退回 `window.api.omm.customGroupRecords(gid)` → `omm2satrec`。`impSatList` 的行 id 对无 NORAD 的星用 `s.key`，NORAD 另存字段。
- 验收：导入 .e 组 → 展开逐颗双击 → 聚焦；组行双击「聚焦全组」→ 全部进选中集；有 / 无 NORAD 两种星都要试。

### 9. 日凌窗没进 before-quit 放行表

- 文件：`electron/main.js:762–769`（六个 `*AllowClose = true`，缺 `_soAllowClose`；守卫在 :299–341）。
- 现象：日凌窗开着时注销 / 关机 / 自动更新「退出时安装」→ 关窗被 `preventDefault` 挡住 → 系统超时强杀或更新装不上（正是该处注释要防的两种后果）。
- 修法：表里加 `_soAllowClose = true`；顺手 grep `AllowClose = false` 与这张表逐个对账，以后新窗口守卫必须同步进表（在表旁加一行注释说明）。
- 验收：grep 对账零缺口；手工：开日凌窗后触发一次 `app.quit()`（临时 dev 钩子即可）能正常退出。

### 10. 日凌窗「历元」读数把不带 Z 的 OMM 历元按本机时间解析

- 文件：`src/suntool/SunOutageApp.vue:305`（`Date.parse(sat.epoch)`）。引擎侧 `packages/core/vendor/satellite.js:1735` 是补 `Z` 按 UTC 解析的。
- 现象：北京机器、星历档、显示时区「本机」→ 历元读数比真值早 8 小时；「距分点 n 天」在日界附近差 1 天。只影响读数行。
- 修法：新建 `src/shared/epochMs.js`：没有 `Z` / `±hh:mm` 结尾的历元串补 `Z` 再 `Date.parse`；`SunOutageApp.vue` 改用它；grep `src/` 里其它 `Date.parse(` 吃 OMM 历元的地方一并换掉。
- 验收：单测：`epochMs('2026-09-20T12:34:56.123456') === Date.UTC(2026,8,20,12,34,56,123)`；带 `Z` / 带偏移的串结果不变。

## 2. P1：应修（读代码判定「很可能」，修前先复现）

### 11. 文件管理导出 SP3 组，得到内容是 STK .e 的 .sp3 文件

- 文件：`src/components/FileManager.vue:158`（把 `g.format = 'sp3'` 原样传给 `customExportGroup`）；`electron/ipc/register.js:303–306`（`saveEph` 按 `format` 取扩展名 `.sp3`、默认名 `…_星历.sp3`）；`electron/services/customSats.js:579–590、:640`（`want === 'sp3' ? 'stk-e'` 静默改写）。仓库没有 SP3 写出器（`ephemFormats.serializeEphemeris` 只有三种）。3D 页的 `impExport` 自己把 sp3→stk-e 映射了，文件管理没有。
- 修法（不写 SP3 写出器）：在 `saveEph` 入口统一把 `sp3` 归一成 `stk-e`（扩展名 `.e`、默认名、返回值 `format` 全部跟着变），`groupText` 里那两处三元就可以删；`FileManager.vue` 传 `g.format === 'sp3' ? 'stk-e' : g.format`；保存对话框的格式列表里不列 SP3。
- 验收：文件管理导出 SP3 导入组 → 文件名 `.e`、内容是 STK .e、返回 `format: 'stk-e'`；单测：`groupText(sp3 组, 'sp3')` 与 `'stk-e'` 输出逐字相同。

### 12. OEM 年积日历元 `YYYY-DDDThh:mm:ss` 不认

- 文件：`packages/core/utils/timeSystems.js:101`（`parseIsoYmd`）、`:136`（`parseEpochLoose`）。CCSDS 502.0-B 明文允许该写法，整份 OEM 会被拒「没有可用的状态矢量段」。
- 修法：两处都接受 `YYYY-DDDThh:mm:ss(.f…)(Z)`：`Date.UTC(y, 0, 1) + (ddd − 1) × 86400e3 + 时分秒`，`ddd` 范围 1–366 按闰年校验。
- 验收：`timeSystems.test.mjs`：`'2026-001T00:00:00.000'` = `Date.UTC(2026,0,1)`；`'2024-366T12:00:00'` 合法、`'2023-366T…'` 报错；`ephemFormats.test.mjs` 加一份年积日体例的 OEM 能解析。

### 13. 导入覆盖未来时段的星历后显示 0 颗，时钟进入时段仍不出现

- 文件：`src/pages/ConstellationMap3D.vue:2136–2144`（`rebuildRenderSet` 的 `add()` 把此刻 `posAt` 为 null 的星直接剔出 `renderEntries`）；时钟推进只跑 `refreshPositions`（:2184），没有时间驱动的重建。
- 修法（二选一，优先 A）：
  - A. `add()` 只剔 SGP4 解算失败的星，星历星一律留在 `renderEntries`；`refreshPositions` 对本拍 `posAt` 为 null 的星不画、不计入 `shownCount`（先确认它现在对 null 的处理，别让 NaN 进 Worker）。
  - B. 在 `refreshPositions` 里按可见星历组的 `[t0,t1]` 记一个「在时段内」集合，跨越边界那一拍调 `rebuildRenderSet()`。
- 验收：导入覆盖明天的 .e → 0 颗；时钟拖进时段 → 出现；拖出 → 消失；`satCount` / `shownCount` 读数跟着变；播放不掉帧。

### 14. 向导点「生成」后卡片 / 轨道线消失

- 文件：`src/pages/ConstellationMap3D.vue:1641–1652`（`saveConstWizard`）、`:1711`（`wizardSolo`）、`:2127–2133`（仅预览分支）、`:2533`（`rebindSelection` 只在 `renderEntries` 里找）。
- 根因：`setPreview(null)` 之后立刻 `customConst.add/update` → 同步 `notify → rebuildRenderSet`，此时 `constModal` 还没清、`pvSolo` 默认开 → 走仅预览分支得到空 `renderEntries` → `rebindSelection` 找不到 → 选中集被丢。
- 修法：调整顺序：`wizPreviewCommit()` → `constModal.value = null`（其 watch 负责撤预览 + 非 solo 重建）→ `add/update` → `rebindSelection('cc_'+id)` → `registerSets()` → `soloSet`。确认 `watch(constModal)`（:1662）在 `_wizCommitted` 已置位时不会把进向导前的选中集还回去。
- 验收：向导里种子星轨道线显示着，点「生成」→ 卡片不关、轨道线不断、卡片名字是提交版本的星；「更新」现有星座同样。

## 3. P2：次要（各一两行就能修，顺手带上）

15. `electron/services/customSats.js:355`：告警去重拿未加前缀的 `w` 查已加前缀的数组，永远查不到；改成按拼好的整句去重，并把同一句跨 N 颗星的告警合并成一条「N 颗：…」。
16. `packages/core/utils/ngsoGeometry.js:1049`（`ephemElements`：`estimatePeriodMin(tab) || (tab.t1 - tab.t0) / 60000`）：表内凑不出两次升交点时拿整段时长冒充周期；改为 `periodMin: null, meanMotion: null` 并带 `periodEstimated: false`，调用方（显示 / 扫描步长）对 null 用缺省步长，不显示编出来的数。
17. `packages/core/utils/gpsAlmanac.js:209`：Ω̇ 与 J2 理论值的告警门限 5% 过紧，真实年历常差 6–7%（日月摄动）；改 15%，措辞去掉「多半是单位或半圆换算弄错了」。
18. `electron/services/solarFlux.js:191–193`：`refresh({force:true})` 遇到飞行中的普通刷新直接返回那个 promise，`force` 被吞；改为等它结束后再跑一次强制刷新（或记一个 pending force）。`solarFlux.test.mjs` 加用例。
19. `electron/ipc/register.js:303–306` + `customSats.js:597–612`：gp 组导出成 STK / OEM 时同步跑 `记录数 × 2881` 次 SGP4 且无上限，万颗组会冻住主进程甚至 OOM；加上限（如 300 颗，超出返回可读错误提示先筛小组），并把默认时窗 / 步长写进对话框返回值。
20. `src/viz/constellation/orbitDesign.js:406`：见第 7 条末尾。

## 4. 工程项

21. `.gitignore`：加 `.covharness/`、`.layersharness/`、`.owizharness/` 三行（其它验证台都进了，只有这三个漏了），各配一行简短注释，风格照文件里现有的验证台条目。
22. **不要**把 `src/viz/grd/whittaker.js`、`packages/core/test/whittaker.test.mjs` 加进你的提交，它们属于另一条工作线；但要提醒用户：已修改的 `useGrdCoverage.js` 和 `package.json` 已经引用它们，那条线提交时漏加会让别人和发版构建直接失败。

---

## 5. 交付要求

1. 每条修复：先写能复现的失败用例（node 能跑的放 `packages/core/test/`，纯界面的写手工步骤），再修，再让用例转绿。
2. 全部做完后 `npm test` 全绿（含新用例）、`npm run build` 通过；把两条命令的结论原样贴出来，失败就贴失败输出，不要总结成「通过」。
3. 每条一次提交或按模块合并提交，`git add` 明确路径；最后 `git status` 贴出来，证明 0.1 节那批未提交文件原样未动。
4. 最后交一张表：`条目 | 状态（已修 / 未修及原因）| 验证方式（用例名或手工步骤）| 提交哈希`。
5. 修不了或判断不该修的条目，写清理由停下，不要绕开做别的。
