node_modules/
out/
dist/
release/
*.log
.DS_Store
.vite/

# Claude Code 本地调试配置（预览用 dev server 启动项等），不进仓库
.claude/

# 离屏验证台（环境场图层的地理配准核对页 + 落盘的场数据，各 1 MB），本地开发用，不进仓库
# .envharness/toggle.mjs 是另一台：图层总开关的取数/铺图时机（node .envharness/toggle.mjs，22 条断言）。
.envharness/

# 离屏验证台（雨衰计算页交互核对，window.api 走桩），本地开发用，不进仓库
# .rainharness/plot/ 是另一台：只挂 RainPlot 的坐标系绘制（node .rainharness/plot/gen.js 重生成）。
# drawTo / xTicksFor 由 gen.js 从 RainPlot.vue 原文抽出、Vue 的 ref/computed 走桩，不是抄一份；
# 曲线点由真引擎 sweepRainAttenuation 预生成。用来肉眼核对刻度标签会不会撞/重复/顶出画布 ——
# 「99.99 99.99 100.00 100.00」这种塌陷单测看不出来，得看。并排画着改前的线性刻度版对拍。
.rainharness/

# 离屏验证台（链路预算图表 / NTN 参数 / 取星面板核对），本地开发用，不进仓库
.lkharness/
.lbntnharness/
.satpickharness/

# 离屏验证台（地理场图：等值线标注 + 可拖色标条，场为合成值），本地开发用，不进仓库
.sfharness/

# 离屏验证台（「详细预算」文档区排版核对：真引擎算例 + 新旧排版对照快照，node .wfharness/gen.mjs 重生成）
.wfharness/

# 离屏验证台（分享 / 导入：两台"机器"并排，A 机勾选出码 → B 机粘码看清单再落盘），本地开发用，不进仓库
.shareharness/

# 在线分享凭证（含密钥，不进仓库；随安装包打包）
electron/services/shareConfig.js

# 和风天气凭证（含 Ed25519 私钥，不进仓库；随安装包打包）
# 生成/体检： node scripts/setup-weather-config.mjs [--genkey|--check]
# 说明与控制台步骤见 electron/services/weatherConfig.example.js
electron/services/weatherConfig.js

# 干扰分析（C/I）UI 验证台：CiApp 挂浏览器里单跑，window.api 由真引擎产出的 fixtures 顶上
.ciharness/

# 离屏验证台（「导出报告」对话框：点框外不关 / ✕ · 取消 · Esc 才关），本地开发用，不进仓库
.rdharness/

# 可见性分析「时间覆盖」读数验证台：真 useVisibility + 真 SGP4（星历取 resources/omm），
# CSS 从 ConstellationMap3D.vue 原文抽注，只用来核对侧栏读数排版；数值正确性归 visTimeCoverage.test.mjs
.visharness/

# PFD Mask 生成器 UI 验证台：PfdApp 挂浏览器里单跑，window.api 走桩，本地开发用，不进仓库
.pfdharness/

# 链路表单元格「行内尾标」（配置名后的实时功放功率）排版验证台：只挂 StationGrid，值直接给不走 IPC
.tagharness/

# 高级计算对话框 SSR 冒烟（node .advharness/ssr.mjs）：八种状态真渲染，查模板运行时错误
# ——build 只查编译期，模板里引用了不存在的变量得靠它。本地开发用，不进仓库
.advharness/

# 频率计划图版式验证台（node .fpharness/gen.mjs 重生成）：双 LO cross-strap 与宽窄混排两张真出图，
# 用来肉眼核对「块宽只由带宽决定」。数值正确性归 packages/core/test/freqPlan.test.mjs 的 ⑩b 段。
# .fpharness/ui/ 是另一台：整个 FreqPlanApp 挂浏览器里跑（window.api 走桩），用来核对单测证明不了的
# DOM 行为：上下行双向联动（改一边另一边跟着变、清一侧两侧都空、cross-strap 解耦互不牵动），
# 以及换单位后整份计划（表/检查器/批量/LO/波束/容量/校验/图）是否一起换刻度、录入中途的小数点会不会被吞。
.fpharness/

# 「发送到小程序」端到端验证台（node .mpharness/gen.mjs）：跨两个仓库真跑一遍——
# 平台造计划/配置 → fpMiniExport/lbMiniExport → 信封 → 小程序 utils/satsimPack（wx.storage 走内存桩）
# → 频率计划页 openPlan/draw（canvas 2d 走录制桩）/tapAt。两侧的字段名编译器一个都管不着，靠它兜。
# 另把绘制指令回放成 SVG 与平台 toSvg 逐图元比对（矩形/直线/圆点/箭头/文字），防「planBody 改了
# drawOps 没跟上」。小程序仓库默认取兄弟目录 ../Satellitelinkbudget，可用 MINIAPP_DIR 覆盖。
.mpharness/

# 频率计划 Excel 导出的版式验证台（node .fpxharness/gen.mjs）：真跑 fpXlsxModel → freqPlanXlsx
# 出两本 .xlsx（样式版 / 纯数据版），并渲成 HTML 预览。数值正确性归 packages/core/test/fpXlsx.test.mjs，
# 这里只看单测证明不了的版式：色块宽度是否随带宽走、量化抖动看不看得出、兑白底色压不压得住字。
.fpxharness/

# 晨昏线验证台：可拖 UTC 时刻，2D 平面图 + 3D 球体同屏画晨昏线/夜区，另附城市昼夜探针。
# 「夜区在不在正确那侧、会不会被地图接缝撕开」的判定已由 packages/core/test/terminatorRender.test.mjs
# 逐点覆盖（进 npm test），这里只留给肉眼看观感（配色/线宽/与覆盖场叠加的层次）。本地开发用，不进仓库
.termharness/

# 交付级报告版式验证台（《技术文档标准模板》口径，三个渲染器共一份版式档 reportStyle.js）。
# 模型三台共用 .rpharness/model.mjs 的 makeModel()，别各带一份。
# node .rpharness/check.mjs        出 out.xlsx / out.docx，拆开 XML 逐项核对——富文本中西分家
#                                  （宋体/黑体 vs TNR）、三线表边框、无底纹无竖线、logo 贴齐版心右边界、
#                                  字号档位、表号连续编号、页眉数、输入表宽。
# node .rpharness/serve.mjs        起 PDF 打印页（out/renderer 的真产物 + 打桩的 window.api，
#                                  需先 npm run build），浏览器里核对版式；对应 launch.json 的 rp-harness。
# npx electron .rpharness/pdf.cjs  真跑 printToPDF 出 out.pdf——页眉的 logo 只有真打印才出现，
#                                  serve.mjs 那台看不到。
# npx electron .rpharness/hdrtest.cjs  页眉模板探针（图不出现时用它分因）。
.rpharness/

# 「帮助 → 微信小程序」介绍弹窗的版式验证台：真组件 + 真全局样式，暗/亮各看一遍（头像反相是否成立）。
.miniaboutharness/

# 星座侧栏「行内展开的卫星列表」验证台（SatList.vue）：三万条真数据，验虚拟滚动只渲染视口内十几行、
# 以及 Excel 式多选口径（点 / Ctrl / Shift / 拖 / Ctrl+A / 方向键）。上方读数带供驱动脚本断言。
.satlistharness/

# 「小程序绑定 · 免密钥自动同步」端到端验证台（node .bindharness/gen.mjs）：
# COS 换内存桩、云函数当普通模块 require 进来真跑一遍——平台 boxSend / 云函数 boxList / 小程序 syncNow
# 三处用各自仓库里的真代码，专治「跨仓库字段名漂了编译器管不着」。
.bindharness/

# 激活管理验证台：smoke.mjs 离线全链路（登录/签发 × 验签/心跳/状态机，share 内存桩）、
# live.mjs 真桶往返（本机心跳 → 管理端读回）、boot.mjs 管理端 Electron 启动探活。
.mgrharness/

# 「星座 → 小程序星座地图」端到端验证台（node .satconharness/gen.mjs）：
# 平台 satconMiniExport 打包 → share.boxSend/putSnapshot → 云函数 → 小程序 importSatSets → 星座地图页
# （Page() 打桩后直接调页面方法），五处各自的真代码跑一遍。
# ★ 核心哨兵：同一条 OMM 记录在平台与小程序两端各自 omm2satrec，satrec 与 +3 天位置必须逐位相同 ——
#   「送根数不送编号」全部的意义就在这一条上，而两边的解析器是同一份代码的两个副本，会漂。
.satconharness/

# 「对星覆盖分析」验证台：SatCovHarness 把壳层投影挂浏览器里单跑（天线网格域切几何 → 逐壳投影），
# SatCovPanelHarness 单挂侧栏面板（window.api 走桩），核对聚焦天线/壳层选择/时段扫描的 DOM 行为。
# 数值正确性归 packages/core/test/shellProj.test.mjs 与 satcovScan.test.mjs。本地开发用，不进仓库
.satcovharness/

# 「端到端链路工作台」验证台：E2eApp 挂浏览器里单跑（window.api 走桩）+ 出图打印页（print-serve.mjs）。
.e2eharness/

# 「帮助 → 关于」窗口版式验证台：真组件 + 真全局样式，暗/亮各看一遍。
.aboutharness/

# 星间链路仿真验证台：模块本身已于 2026-08-12 回退，台子留着备查，不进仓库。
.islharness/

# 配置列表树验证台（ConfigTree.vue + ConfigTreeMenu.vue）：真组件 + 真全局样式、无 IPC。
# 盯层级几何（名称起点随深度严格单调）、导引线连续性、拖拽三区与悬停展开、焦点/载入/剪切三态叠加。
# 带「旧几何」开关按改动前的算式复现，用来对拍。
.cfgtreeharness/

# Excel 网格验证台（ExcelGrid.vue + useGridSelect.js）：序号列/列头/全选、填充柄与 Ctrl+D、
# 右键菜单定位、列宽拖拽与双击自适应、只读表列头排序、sticky 之下的 ensureVisible。
# 读数行把选区/排序/列宽实时打出来，便于脚本断言。
.gridharness/

# 标记「航迹分页」验证台：主从两栏（左航迹 / 右航点网格）+ 真 useMarkerTable / trajsFromSheets。
# ★ 窗口外壳与 .mk-* / .mtj-* 样式抄自 ConstellationMap3D.vue，改一处两处同步。
.mkharness/

# 3D 球体截图导出验证台：真 createGlobeScene + 真 exportGlobe，验 scene.snapshot 拿回的确实是
# 「按倍率放大的那一帧」而不是黑图/拉伸图，并量一遍耗时与体积（页面直接把出的图贴出来看）。
.shotharness/

# 离屏验证台（图层显隐拨杆 .lsw：环境场总开关条 + 标记三分区标题，浅/深色 × 开/关四态并排），本地开发用，不进仓库
.lswharness/
# 离屏验证台（全平台控件基线：复选框/单选钮/拨杆/滑块/下拉/取色/分段/滚动条，浅深双主题并排），本地开发用，不进仓库
.ctlharness/
# 离屏验证台（GRD 天线设置面板的行距/对齐实测），本地开发用，不进仓库
# .grdharness/swatch.html 是另一台（node .grdharness/serve.js，launch.json 的 grd-swatch-harness）：
# 「显示选项」逐项色块 —— 色块钉在行尾成一列、点色块不会连带切掉勾选（它在 <label> 里）、
# 长标签换行时不被裁掉。中/英 × 浅/深四态并排，页面自带断言读数行。
.grdharness/

# 「显示设置 · 聚焦卫星」验证台：真模板（node .focusharness/gen.mjs 从 ConstellationMap3D.vue 抽出）
# + 真全局样式 + 真 Icon，浅/深两主题看版式。几何数值归 packages/core/test 与两台离屏脚本。
.focusharness/

# 时间控制条验证台：真模板 + 真样式（node .tlharness/gen.mjs 从 ConstellationMap3D.vue 抽出）。
# 页面里点「扫一遍偏移量」即量出读数块/刻度尺/右组在偏移量长短变化下的宽度与左沿——拖游标抖不抖看这个。
.tlharness/

# 底图/行政区构建脚本的原始数据缓存（Natural Earth / geoBoundaries 下载件），不进仓库
scripts/_ne/

# 底图边界线验证台：2D 四档缩放 + 三处放大对照（克什米尔/南海/藏南）+ 3D 球体，本地开发用，不进仓库
.geoharness/

# 实时气象场验证台：GFS 栅格 + 气象业务色阶 → 2D/3D 双视图，肉眼核对地理配准与云图观感。
# 落盘的 .f32/.json 是几十 MB 的现取气象数据，本地开发用，不进仓库
.lvharness/

# 地球站地图符号验证台：现状（写实插画）与 A/B 两个符号方案并排画在真实底图上
# （海 #15426b · 陆 #e4eccf · 文字 casing rgba(6,11,18,.82)），走与实现一致的
# SVG→Image→drawImage 位图路径，看的就是「缩到 20px 时还剩下什么」。本地开发用，不进仓库
.stharness/

# 航迹载具符号（船舶 / 飞机）验证台：三张图 —— ① 各档大小 × 海/陆底色的符号本体（2D canvas 路径，
# 与 3D 贴图并排对拍）；② 真 createFlatCoverage 走 exportRender 的 compat 子路径（＝导出那条，
# 不认 Path2D），看图标落在航迹头上、朝向贴着折线；③ 真 createGlobeScene，八条方位各差 45° 的
# 航迹摆成风车，看 3D 那条「球面切向投到屏幕」的朝向算得对不对。页面自己把图 POST 到 shots/。
.vehharness/

# 地图注记可读性对照台（npx vite --config .readharness/vite.config.mjs）：现状 vs 逐项修复并排
.readharness/

# 界面字体验证台（设置 → 界面字体）：真 SettingsModal + 真全局样式，摆一组典型界面样张（侧栏参数行 /
# 11px 密排链路表 / 中西文混排 / 报表面），换一档字体当场看中西文齐不齐、密排还读不读得出、数字列还对不对得齐。
.fontharness/

# 「地图设置 → 地名 → 透明度」验证台（npx vite --config .nameopharness/vite.config.mjs）：挂真的 flatCoverage，
# 逐档量「注记相对无注记底图的压深/提亮/着墨像素数」，守住「三项均单调递减、op=0 恰好回到底图」。
.nameopharness/

# 水域注记 + 岛链验证台（npx vite --config .waterharness/vite.config.mjs）：2D 全球 + 2D 西太平洋近景 + 3D 球体
# 三处同时打开大洋/海域两档，肉眼核对「名字落在水面上、层级随缩放逐档冒出、两个渲染器同一份表」。
# 另一台（vite.app.config.mjs，无 IPC 的主窗口整页）只为核对「地图设置 → 地名」那一节的版面。
# 岛链在两个视图里走向一致（顶点在经纬度平面加密过）也在这里看。
# 位置对不对由 packages/core/test/mapAnnotations.test.mjs 逐点判陆地面，不靠肉眼。
.waterharness/

# 「编辑卫星」弹窗实时生效验证台（npx vite --config .satedharness/vite.config.mjs，launch.json 的 satedit-harness）：
# 模板 / 样式 / 逻辑三段由 .satedharness/extract.mjs 从 ConstellationMap3D.vue 原样抠出（不抄一遍，锚点挪了就抛），
# 外面套假 grd 存储 + 假地图，点「跑自动检查」跑 20 条：回车 / 失焦 / 滑块 / 勾选 / 取色是否当场落库、
# 半截输入（只打一个负号）不许落库、滑块每帧至多提交一次且不惊动壳层、取消逐字段回滚、保存留下。
# ★ 预览窗藏起来时浏览器把 rAF 整个停掉（被测代码那一帧合并就永远等不到），台子里给 rAF 垫了 120ms 兜底。
.satedharness/

# 显示时区档位验证台（npx vite --config .tzharness/vite.config.mjs，launch.json 的 tz-harness）：
# TzPicker 挂在三个真宿主上（时间条读数块 / 过境时基行角标 / 链路预算几何卡角标），CSS 从
# ConstellationMap3D.vue 与 NgsoLinkBudgetApp.vue 的 <style scoped> 原文抽出注入。
# 「换档位不许改读数块宽」那条在台子里一键量（换档必须 await nextTick 再量，否则量的是上一帧）。
# 数值口径（往返 / 夏令时 / 午夜对齐）由 packages/core/test/tzMode.test.mjs 管，不靠肉眼。
.tzharness/

# 离屏验证台（标记层：九种符号 / 逐条颜色 / 标注四方位 / 直接拖拽；2D 真渲染器 + 3D 真球体两页），本地开发用，不进仓库
.mkstyleharness/

# 2D 渲染性能对拍台（npx vite --config .perfharness/vite.config.mjs，launch.json 的 perf-harness）：
# 同一份内容在【当前工作区】与【v1.4.1 快照】两个 flatCoverage 上各画一遍，量同步整帧耗时与
# 静态层重建次数（base/ cur/ 两份 src 快照 + renderStaticLayers 里插的计数器）。
# ★ 预览窗藏起来时 rAF 被节流到 ~1Hz，台子把 requestAnimationFrame 换成手工队列同步 flush；
#   ResizeObserver 同样不发，进场必须显式 flat.resize()，否则 cw/ch 还是 0、draw() 直接返回。
# ★ 两版不能一前一后连着量：先量的那个会替后面那个背走上一轮积压的 GPU 光栅，实测能凭空造出 1.35×。
.perfharness/

# 影像瓦片离线包（出厂 L0–L6 = 4273 片 / 100 MB，由 npm run imagery:tiles 生成，不进 git）
# ★ 它是【出厂默认底图】（viz/imagery.js 的 DEFAULT_IMAGERY = 'bmTiles'），漏带不会报错、只会让所有
#   用户的「高精」档静默回退成矢量底图 —— 故 npm run dist 前置了 scripts/check-imagery.mjs 逐级点数。
resources/imagery/
.imagery-src/

# 影像瓦片验证台（npx vite --config .imgharness/vite.config.mjs）：把 resources/imagery 挂成静态 publicDir，
# 用 http://localhost:5933/bmng/<z>/<行>/<列>.jpg 顶替 imagery:// —— 协议注册在主进程，浏览器里没有它，
# 这正是 imageryTiles.setTileBase 存在的理由。网格数学与选级由 packages/core/test/imageryTiles.test.mjs 管。
.imgharness/

# MODCOD 表编辑页验证台（npx vite --config .mcharness/vite.config.mjs，launch.json 的 mc-harness）：
# 真 ExcelGrid + 真 useGridSelect 挂在假 window.api.modcod 上（stub.js），量枚举列下拉、粘贴/填充柄那条
# 旁路、「按族 + 星座阶数 M 现造一档」的插槽。数值口径与 Excel 往返由 modcodTables / modcodXlsx 两个
# 测试管，台子只管交互。
.mcharness/
