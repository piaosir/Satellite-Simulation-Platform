node_modules/
out/
dist/
release/
*.log
.DS_Store
.vite/

# Claude Code 本地调试配置（预览用 dev server 启动项等），不进仓库
.claude/

# 离屏验证台（环境场图层的地理配准核对页 + 落盘的场数据，各 1 MB），本地开发用，不进仓库
.envharness/

# 离屏验证台（雨衰计算页交互核对，window.api 走桩），本地开发用，不进仓库
.rainharness/

# 离屏验证台（链路预算图表 / NTN 参数 / 取星面板核对），本地开发用，不进仓库
.lkharness/
.lbntnharness/
.satpickharness/

# 离屏验证台（地理场图：等值线标注 + 可拖色标条，场为合成值），本地开发用，不进仓库
.sfharness/

# 离屏验证台（「详细预算」文档区排版核对：真引擎算例 + 新旧排版对照快照，node .wfharness/gen.mjs 重生成）
.wfharness/

# 离屏验证台（分享 / 导入：两台"机器"并排，A 机勾选出码 → B 机粘码看清单再落盘），本地开发用，不进仓库
.shareharness/

# 在线分享凭证（含密钥，不进仓库；随安装包打包）
electron/services/shareConfig.js

# 干扰分析（C/I）UI 验证台：CiApp 挂浏览器里单跑，window.api 由真引擎产出的 fixtures 顶上
.ciharness/

# 离屏验证台（「导出报告」对话框：点框外不关 / ✕ · 取消 · Esc 才关），本地开发用，不进仓库
.rdharness/

# 可见性分析「时间覆盖」读数验证台：真 useVisibility + 真 SGP4（星历取 resources/omm），
# CSS 从 ConstellationMap3D.vue 原文抽注，只用来核对侧栏读数排版；数值正确性归 visTimeCoverage.test.mjs
.visharness/

# PFD Mask 生成器 UI 验证台：PfdApp 挂浏览器里单跑，window.api 走桩，本地开发用，不进仓库
.pfdharness/

# 链路表单元格「行内尾标」（配置名后的实时功放功率）排版验证台：只挂 StationGrid，值直接给不走 IPC
.tagharness/

# 高级计算对话框 SSR 冒烟（node .advharness/ssr.mjs）：八种状态真渲染，查模板运行时错误
# ——build 只查编译期，模板里引用了不存在的变量得靠它。本地开发用，不进仓库
.advharness/

# 频率计划图版式验证台（node .fpharness/gen.mjs 重生成）：双 LO cross-strap 与宽窄混排两张真出图，
# 用来肉眼核对「块宽只由带宽决定」。数值正确性归 packages/core/test/freqPlan.test.mjs 的 ⑩b 段。
# .fpharness/ui/ 是另一台：整个 FreqPlanApp 挂浏览器里跑（window.api 走桩），用来核对单测证明不了的
# DOM 行为：上下行双向联动（改一边另一边跟着变、清一侧两侧都空、cross-strap 解耦互不牵动），
# 以及换单位后整份计划（表/检查器/批量/LO/波束/容量/校验/图）是否一起换刻度、录入中途的小数点会不会被吞。
.fpharness/

# 「发送到小程序」端到端验证台（node .mpharness/gen.mjs）：跨两个仓库真跑一遍——
# 平台造计划/配置 → fpMiniExport/lbMiniExport → 信封 → 小程序 utils/satsimPack（wx.storage 走内存桩）
# → 频率计划页 openPlan/draw（canvas 2d 走录制桩）/tapAt。两侧的字段名编译器一个都管不着，靠它兜。
# 另把绘制指令回放成 SVG 与平台 toSvg 逐图元比对（矩形/直线/圆点/箭头/文字），防「planBody 改了
# drawOps 没跟上」。小程序仓库默认取兄弟目录 ../Satellitelinkbudget，可用 MINIAPP_DIR 覆盖。
.mpharness/

# 频率计划 Excel 导出的版式验证台（node .fpxharness/gen.mjs）：真跑 fpXlsxModel → freqPlanXlsx
# 出两本 .xlsx（样式版 / 纯数据版），并渲成 HTML 预览。数值正确性归 packages/core/test/fpXlsx.test.mjs，
# 这里只看单测证明不了的版式：色块宽度是否随带宽走、量化抖动看不看得出、兑白底色压不压得住字。
.fpxharness/

# 晨昏线验证台：可拖 UTC 时刻，2D 平面图 + 3D 球体同屏画晨昏线/夜区，另附城市昼夜探针。
# 「夜区在不在正确那侧、会不会被地图接缝撕开」的判定已由 packages/core/test/terminatorRender.test.mjs
# 逐点覆盖（进 npm test），这里只留给肉眼看观感（配色/线宽/与覆盖场叠加的层次）。本地开发用，不进仓库
.termharness/

# 交付级报告版式验证台（《技术文档标准模板》口径，三个渲染器共一份版式档 reportStyle.js）。
# 模型三台共用 .rpharness/model.mjs 的 makeModel()，别各带一份。
# node .rpharness/check.mjs        出 out.xlsx / out.docx，拆开 XML 逐项核对——富文本中西分家
#                                  （宋体/黑体 vs TNR）、三线表边框、无底纹无竖线、logo 贴齐版心右边界、
#                                  字号档位、表号连续编号、页眉数、输入表宽。
# node .rpharness/serve.mjs        起 PDF 打印页（out/renderer 的真产物 + 打桩的 window.api，
#                                  需先 npm run build），浏览器里核对版式；对应 launch.json 的 rp-harness。
# npx electron .rpharness/pdf.cjs  真跑 printToPDF 出 out.pdf——页眉的 logo 只有真打印才出现，
#                                  serve.mjs 那台看不到。
# npx electron .rpharness/hdrtest.cjs  页眉模板探针（图不出现时用它分因）。
.rpharness/

# 「帮助 → 微信小程序」介绍弹窗的版式验证台：真组件 + 真全局样式，暗/亮各看一遍（头像反相是否成立）。
.miniaboutharness/

# 星座侧栏「行内展开的卫星列表」验证台（SatList.vue）：三万条真数据，验虚拟滚动只渲染视口内十几行、
# 以及 Excel 式多选口径（点 / Ctrl / Shift / 拖 / Ctrl+A / 方向键）。上方读数带供驱动脚本断言。
.satlistharness/

# 「小程序绑定 · 免密钥自动同步」端到端验证台（node .bindharness/gen.mjs）：
# COS 换内存桩、云函数当普通模块 require 进来真跑一遍——平台 boxSend / 云函数 boxList / 小程序 syncNow
# 三处用各自仓库里的真代码，专治「跨仓库字段名漂了编译器管不着」。
.bindharness/

# 激活管理验证台：smoke.mjs 离线全链路（登录/签发 × 验签/心跳/状态机，share 内存桩）、
# live.mjs 真桶往返（本机心跳 → 管理端读回）、boot.mjs 管理端 Electron 启动探活。
.mgrharness/

# 「星座 → 小程序星座地图」端到端验证台（node .satconharness/gen.mjs）：
# 平台 satconMiniExport 打包 → share.boxSend/putSnapshot → 云函数 → 小程序 importSatSets → 星座地图页
# （Page() 打桩后直接调页面方法），五处各自的真代码跑一遍。
# ★ 核心哨兵：同一条 OMM 记录在平台与小程序两端各自 omm2satrec，satrec 与 +3 天位置必须逐位相同 ——
#   「送根数不送编号」全部的意义就在这一条上，而两边的解析器是同一份代码的两个副本，会漂。
.satconharness/

# 「对星覆盖分析」验证台：SatCovHarness 把壳层投影挂浏览器里单跑（天线网格域切几何 → 逐壳投影），
# SatCovPanelHarness 单挂侧栏面板（window.api 走桩），核对聚焦天线/壳层选择/时段扫描的 DOM 行为。
# 数值正确性归 packages/core/test/shellProj.test.mjs 与 satcovScan.test.mjs。本地开发用，不进仓库
.satcovharness/

# 「端到端链路工作台」验证台：E2eApp 挂浏览器里单跑（window.api 走桩）+ 出图打印页（print-serve.mjs）。
.e2eharness/

# 「帮助 → 关于」窗口版式验证台：真组件 + 真全局样式，暗/亮各看一遍。
.aboutharness/

# 星间链路仿真验证台：模块本身已于 2026-08-12 回退，台子留着备查，不进仓库。
.islharness/
